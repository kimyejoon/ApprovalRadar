# ApprovalRadar 사용자 매뉴얼

ApprovalRadar는 **식품안전나라 공공데이터 API**를 활용하여 전국의 식품위생업소 인허가 변동사항(신규 인허가, 대표자 변경, 휴/폐업 등)을 실시간으로 감지하고 모니터링할 수 있는 로컬 데스크톱 전용 프로그램입니다.

---

## 1. 설치 및 실행 방법

이 프로그램은 복잡한 설치 과정 없이 단일 실행 파일로 제공됩니다.

### 🖥️ Windows 사용자
1. 배포받은 폴더(`dist_release`) 내의 `ApprovalRadar.exe` 파일을 더블클릭하여 실행합니다.
2. 검은색 터미널 창 또는 런처 UI가 나타납니다.
3. 런처 화면의 **[브라우저 열기]** 버튼을 클릭하거나, 웹 브라우저 주소창에 `http://localhost:8001`을 입력하여 대시보드에 접속합니다.

### 🍎 macOS 사용자
1. 배포받은 폴더(`dist_release`) 내의 `ApprovalRadar` 바이너리 파일을 실행합니다.
2. *주의: 인터넷에서 다운로드한 프로그램 보안 경고가 뜰 경우*
   - `시스템 설정` > `개인정보 보호 및 보안`으로 이동합니다.
   - 하단 보안 섹션에서 `확인 없이 열기`를 클릭하여 실행을 허용합니다.
3. 실행 후 터미널 창에 서버 시작 메시지가 나타나면 웹 브라우저 주소창에 `http://localhost:8001`을 입력하여 접속합니다.

---

## 2. 주요 기능 및 사용법

### 📊 1) 메인 대시보드 (실시간 모니터링)
- **실시간 데이터 조회**: 수집된 인허가 정보를 최신순으로 확인할 수 있습니다.
- **하이라이트 표시**: 새롭게 업데이트되어 아직 확인하지 않은 내역은 **초록색 테두리/배경**으로 강조 표시됩니다. 클릭 시 "읽음" 상태로 변경됩니다.
- **검색 및 필터링**: 지역, 날짜, 업종별로 원하는 데이터만 빠르게 필터링할 수 있습니다.
- **엑셀 다운로드 (Export)**: 조회 중인 데이터를 엑셀(`.xlsx`) 파일로 추출하여 보고서 작업 등에 활용할 수 있습니다.

### 🔔 2) 실시간 알림 (Pop-up & Sound)
- 프로그램이 켜져 있는 동안 시스템은 자동으로 최신 인허가 정보를 탐색합니다. (기본 30분 주기)
- **변동사항 발생 시**: 
  - 경쾌한 **알림음**이 울립니다.
  - 화면 우측 하단에 **[신규/상태변경/대표자변경]** 요약 정보가 포함된 토스트 팝업이 나타납니다.
  - *참고: 시스템 연결이 끊기거나 크롤링 에러가 발생한 경우 에러 복구 팝업도 제공됩니다.*

### 📝 3) 상세 정보 및 메모 기능
- 대시보드의 행(Row)을 클릭하면 **상세 정보 모달 창**이 열립니다.
- 상세 창에서는 **연락처, 이전 대표자 이름, 이전 영업 상태, 세부 업종** 등을 파악할 수 있습니다.
- **메모장 기능**: 각 업소별로 진행 상황이나 연락 기록 등을 기록할 수 있는 메모 기능을 제공하며, 작성된 메모는 시스템에 안전하게 저장됩니다.

### 🔑 4) API 키 관리 및 자동 교체 (Key Rotation)
- **설정(Settings)** 메뉴에서 식품안전나라 공공데이터 API 키를 관리할 수 있습니다.
- **API 한도 자동 대응**: 공공데이터 API는 일일 호출 한도(예: 1,000건)가 정해져 있습니다. 한도가 소진되면 프로그램이 등록된 **다음 API 키로 자동으로 교체(Rotation)**하여 모니터링이 중단되지 않도록 보장합니다.
- 키별 오늘 사용량과 현재 상태(활성/한도초과)를 한눈에 볼 수 있습니다.

### 🛠 5) 시스템 로그 및 상태 모니터링 (Log Page)
- **로그 페이지**에서 현재 데이터 수집기(크롤러)가 어떻게 동작하고 있는지 실시간 텍스트로 확인할 수 있습니다.
- 오류가 발생했거나 특정 시점에 데이터를 얼마나 가져왔는지 추적할 때 유용합니다.

---

## 3. 자주 묻는 질문 (FAQ) & 유의사항

**Q. 프로그램 창을 끄면 어떻게 되나요?**
A. 프로그램 실행 창(또는 터미널)을 종료하면 서버와 데이터 수집 로직이 모두 종료됩니다. 모니터링 알림을 지속적으로 받으시려면 프로그램 창을 최소화하여 항상 켜두셔야 합니다.

**Q. 과거 데이터도 모두 볼 수 있나요?**
A. 본 시스템은 "실시간 변동" 모니터링에 특화되어 있습니다. 프로그램을 켜둔 시점부터 최신 데이터를 추적하여 데이터베이스에 누적합니다. 프로그램 최초 실행 시 기본적인 과거 베이스라인 데이터를 수집하는 시간이 약간 소요될 수 있습니다.

**Q. 화면에 데이터가 갑자기 안 나옵니다.**
A. 설정된 API 키가 모두 소진되었거나, 공공데이터포털 서버의 일시적인 장애일 수 있습니다.
  - 먼저 **[설정] > [API 키 관리]**에서 모든 키가 '한도 초과(Exhausted)' 상태인지 확인하세요.
  - 모든 키가 소진된 경우 자정이 지나면 자동으로 한도가 리셋되어 데이터 수집이 재개됩니다. 여분의 키를 추가로 등록해 두시면 좋습니다.

**Q. 비밀번호나 데이터가 외부로 유출되지 않나요?**
A. ApprovalRadar는 1인 사용자를 위한 로컬 애플리케이션입니다. 모든 데이터와 메모, API 키는 사용자의 PC 내장 데이터베이스(`food_safety.db`)에만 저장되며, 외부 서버로 일절 전송되지 않습니다.

---

*본 프로그램은 안정적인 모니터링을 위해 수시로 업데이트를 확인하고, 공공데이터 트래픽 규정(WAF)을 준수하도록 설계되었습니다.*
