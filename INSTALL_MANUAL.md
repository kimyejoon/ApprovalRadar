# ApprovalRadar — 설치 및 사용 매뉴얼

> **대상:** 최종 사용자 (설치 담당자)  
> **버전:** 2026-05-17 기준

---

## 1. 패키지 구성

수령한 압축 파일을 풀면 아래 파일들이 들어 있습니다:

```
ApprovalRadar/
├── ApprovalRadar          ← 실행 파일 (macOS)
├── ApprovalRadar.exe      ← 실행 파일 (Windows)
├── .env                   ← API 키 설정 파일
├── food_safety.db         ← 인허가 데이터 저장소
└── 실행방법.txt           ← 간단 안내
```

> ⚠️ **이 폴더 내 파일 구조를 변경하지 마세요.** 실행 파일은 같은 폴더에 있는 `.env`와 `food_safety.db`를 참조합니다.

---

## 2. macOS 설치 및 실행

### 2-1. 압축 해제

다운로드한 `.zip` 파일을 원하는 위치에 압축 해제합니다.

> **권장 위치:** `~/Documents/ApprovalRadar/`  
> (iCloud Drive 동기화 폴더는 피해주세요 — 속도 저하 가능)

### 2-2. 첫 실행 (보안 설정)

macOS는 인증되지 않은 앱을 차단합니다. **최초 1회만** 아래 설정이 필요합니다.

**방법 A — 우클릭 실행 (권장):**
1. `ApprovalRadar` 파일을 **우클릭(Control+클릭)**
2. "열기" 선택
3. 경고 팝업에서 "열기" 클릭

**방법 B — 시스템 설정:**
1. `ApprovalRadar` 더블클릭 → 차단 경고 확인
2. **시스템 설정** → **개인정보 보호 및 보안**
3. 하단 "확인 없이 열기" 또는 "허용" 클릭

**방법 C — 터미널 (가장 확실):**
```bash
xattr -cr ~/Documents/ApprovalRadar/ApprovalRadar
```
이후 더블클릭으로 정상 실행

### 2-3. 정상 실행 화면

1. 런처 창이 열립니다 (상태/로그 표시)
2. 브라우저가 자동으로 `http://localhost:8000` 을 엽니다
3. 잠시 후 대시보드가 로드됩니다

### 2-4. 종료

런처 창의 **[종료]** 버튼을 클릭합니다. (창을 그냥 닫으면 백그라운드에서 계속 실행됩니다)

---

## 3. Windows 설치 및 실행

### 3-1. 압축 해제

다운로드한 `.zip` 파일을 원하는 위치에 압축 해제합니다.

> **권장 위치:** `C:\ApprovalRadar\`

### 3-2. 첫 실행 (SmartScreen 설정)

Windows Defender SmartScreen 경고가 뜰 수 있습니다:

1. `ApprovalRadar.exe` 더블클릭
2. "Windows가 PC를 보호했습니다" 팝업 → **"추가 정보"** 클릭
3. **"실행"** 클릭

### 3-3. 정상 실행

macOS와 동일합니다. 런처 창 → 브라우저 자동 오픈.

---

## 4. API 키 설정

`.env` 파일을 텍스트 편집기로 열어 API 키를 입력합니다.

```
# macOS: 텍스트 편집기, TextEdit, VS Code 등
# Windows: 메모장(notepad), 메모장++ 등
```

```env
FOOD_SAFETY_API_KEYS=키1번,키2번,키3번,키4번,키5번
```

> **API 키 발급:** [공공데이터포털](https://www.data.go.kr) → "식품안전나라 인허가" 검색 → 활용신청  
> API 키는 콤마(`,`)로 구분하여 여러 개 입력 가능합니다 (최대 5개 권장)

---

## 5. 데이터베이스 (`food_safety.db`)

### 데이터 현황

| 항목 | 내용 |
|------|------|
| 식품업소 인허가변경 (I2859) | 약 77,000건 |
| 음식점업소 인허가변경 (I2861) | 약 327,000건 |
| 합계 | **약 404,000건** |

### 백업 방법

중요한 데이터는 정기적으로 백업하세요:

```bash
# macOS
cp ~/Documents/ApprovalRadar/food_safety.db ~/Desktop/food_safety_backup_$(date +%Y%m%d).db

# Windows (cmd)
copy C:\ApprovalRadar\food_safety.db C:\ApprovalRadar\food_safety_backup_%date:~0,4%%date:~5,2%%date:~8,2%.db
```

### 주의사항

- `food_safety.db` 파일을 **삭제하면 모든 데이터가 초기화**됩니다
- 앱이 실행 중인 상태에서 DB를 복사/이동하지 마세요 (데이터 손상 위험)
- 앱 종료 후 백업하는 것을 권장합니다

---

## 6. 업데이트 방법

새 버전 배포 시:

1. 현재 앱 **종료**
2. `food_safety.db` 를 안전한 곳에 **백업**
3. 새 버전 압축 해제
4. 백업해둔 `food_safety.db` 를 새 폴더에 **복사** (덮어쓰기)
5. `.env` 파일 내용을 새 버전의 `.env` 에 **붙여넣기** (API 키 유지)
6. 실행

---

## 7. 문제 해결

### 브라우저가 자동으로 열리지 않을 때

직접 브라우저를 열고 주소창에 입력:
```
http://localhost:8000
```

### 포트 충돌 (8000번 포트 사용 중)

앱이 자동으로 8001~8010번 포트를 시도합니다. 런처 창에 실제 포트가 표시됩니다.

### 앱이 응답하지 않을 때

1. 런처 창에서 [종료] 클릭
2. 작업 관리자(Windows) / 활성 상태 보기(macOS)에서 `ApprovalRadar` 프로세스 강제 종료
3. 재실행

### 데이터가 오래됐을 때 (수동 동기화)

앱이 30분마다 자동 동기화하지만, 즉시 동기화가 필요한 경우:
- 관리자 페이지 → 크롤러 수동 실행

---

## 8. 시스템 요구사항

| 항목 | macOS | Windows |
|------|-------|---------|
| OS 버전 | macOS 12 (Monterey) 이상 | Windows 10 이상 |
| CPU | Intel / Apple Silicon | x64 |
| RAM | 최소 2GB | 최소 2GB |
| 저장공간 | 500MB 이상 (DB 포함) | 500MB 이상 |
| 네트워크 | 필수 (크롤링용) | 필수 |
| 브라우저 | Chrome, Safari, Edge 등 | Chrome, Edge 등 |

---

## 9. 연락처

운영 중 문제가 발생하면 개발팀에 문의하세요.

- 이슈 리포트 시 런처 창의 로그 내용을 함께 제공해주세요
- `food_safety.db` 파일 크기 및 최근 업데이트 날짜 정보도 포함해주세요

